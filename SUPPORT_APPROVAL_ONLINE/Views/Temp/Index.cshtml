@model IEnumerable<SUPPORT_APPROVAL_ONLINE.Models.tbl_User>
@using SUPPORT_APPROVAL_ONLINE.Models
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var user = Session["user"] as tbl_User;
    //var totalRecord = Model.Where(r => r.userId_Approval == user.user_Id).Count();
}
@Html.ActionLink("Danh sách yêu cầu","ListRequest")

<div id="test"></div>
<div class="x_panel">
    <div class="x_title">
        <h2>Danh sách tài khoản</h2>
        <ul class="nav navbar-right panel_toolbox">
            <li>
                <a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
            </li>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
                <ul class="dropdown-menu" role="menu">
                    <li>
                        <a href="#">Settings 1</a>
                    </li>
                    <li>
                        <a href="#">Settings 2</a>
                    </li>
                </ul>
            </li>
            <li>
                <a class="close-link"><i class="fa fa-close"></i></a>
            </li>
        </ul>
        <div class="clearfix"></div>
    </div>
    <div class="x_content">
        <table id="datatable" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.U_username)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.tbl_Group.group_Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.tbl_Permission.allow)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.U_fullname)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.U_phone)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.U_email)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
            {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.U_username)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.tbl_Group.group_Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.tbl_Permission.allow)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.U_fullname)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.U_phone)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.U_email)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.U_Id }) |
                            @Html.ActionLink("Details", "Details", new { id = item.U_Id }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.U_Id })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<script>
    $("select").change(function () {
        $.ajax({
            url: "@Url.Action("Index", "Sakura")",
            type: 'GET',
            dataType: "html",
            async: true,
            success: function (data) {
                alert("a");
                $("#test").html(data);
            }
        });
    });

</script>